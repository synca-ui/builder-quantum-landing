# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
.env

# MCP-Server-Konfiguration: enthält den n8n-API-Schlüssel.
# Dieses Repo ist öffentlich – die Datei darf NIE eingecheckt werden.
.mcp.json
maitr-promo.mp4
rec.mov

# DB-Exporte mit echten Kundendaten — gehören nie ins (öffentliche) Repo
WebApp.json

# Lokale Claude-Code-Konfiguration (maschinenspezifisch, u. a. settings.local.json)
.claude/

# PP Frama (Pangram Pangram) liegt uns nur als "Free for Personal Use"-Fassung vor.
# Dieses Repo ist OEFFENTLICH: eingecheckte Schriftdateien waeren Weiterverteilung
# und damit ein Lizenzverstoss. Die Dateien wurden deshalb aus der Versionskontrolle
# genommen; lokal duerfen sie liegen bleiben (persoenliche Nutzung), fuer Builds
# reicht das. Ohne sie faellt die App auf die System-Grotesk zurueck
# (siehe mobile/src/theme/fonts.ts).
#
# Bewusst OHNE Pfadpraefix: Muster ohne Schraegstrich greifen in JEDEM Verzeichnis.
# Das ist noetig, weil `expo prebuild` die Schriften zusaetzlich nach
# mobile/android/app/src/main/assets/fonts/ und mobile/ios/ kopiert - ein Muster nur
# fuer mobile/assets/fonts/ liess diese Kopien durch (genau so kamen sie ins Repo).
PPFrama*.otf
PPFrama*.ttf
PPFrama*.woff
PPFrama*.woff2

# OFL-lizenzierte Schriften (z. B. Familjen Grotesk, Bricolage Grotesque) duerfen
# mitverteilt werden und sind daher NICHT ausgeschlossen. Die OFL erlaubt auch Repo-Hosting.
