# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.config/
.env

# MCP-Server-Konfiguration: enthält den n8n-API-Schlüssel.
# Dieses Repo ist öffentlich – die Datei darf NIE eingecheckt werden.
.mcp.json
maitr-promo.mp4
rec.mov

# DB-Exporte mit echten Kundendaten — gehören nie ins (öffentliche) Repo
WebApp.json

# Lokale Claude-Code-Konfiguration (maschinenspezifisch, u. a. settings.local.json)
.claude/

# PP Frama liegt hier als "Free for Personal Use"-Version (Pangram Pangram).
# Dieses Repo ist oeffentlich -> die OTF-Dateien duerfen nicht mitverteilt werden.
# Fuer Builds lokal ablegen; ohne sie faellt die App auf die System-Grotesk zurueck
# (siehe mobile/src/theme/fonts.ts, EXPO_PUBLIC_BRAND_FONT=off).
mobile/assets/fonts/*.otf
mobile/assets/fonts/*.ttf
